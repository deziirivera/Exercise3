Pkg.add("PyCall")
Pkg.build("PyCall")
using PyCall
Pkg.add("PyPlot")
Pkg.build("PyPlot")
using PyPlot

#1a
elcapitan = imread("el-capitan.png")
imshow(convert(Array{Float32,3}, elcapitan))
size(elcapitan)

#1b
"""
load_image -- function should load image and extract red, blue, and green channels
as separate variables channels as the .


Args:
   image-- the name of the RBG image as a string

Returns:
   the matrices created, which I called red, green, and blue
"""
function load_image(image)
    name = imread(image)
    imshow(convert(Array{Float32,3}, name))

    red = name[:,:,1]
    green = name[:,:,2]
    blue = name[:,:, 3]
    return red, green, blue
end
